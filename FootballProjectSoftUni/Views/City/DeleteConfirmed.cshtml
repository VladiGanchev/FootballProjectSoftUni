@using FootballProjectSoftUni.Core.Models.City
@model DeleteCityViewModel

@{
    ViewBag.Title = "Delete City";
}

<section class="container py-4">

    <div class="text-center mb-4">
        <h2 class="page-title text-danger mb-1">
            <i class="bi bi-trash-fill me-2"></i>
            @ViewBag.Title
        </h2>
        <p class="text-muted mb-0">
            Потвърди изтриването. Това действие не може да бъде върнато.
        </p>
    </div>

    <div class="admin-form-card mx-auto">

        <div class="text-center">
            <div class="delete-icon">
                <i class="bi bi-exclamation-triangle-fill"></i>
            </div>

            <h3 class="mt-3 mb-2 fw-bold">@Model.Name</h3>

            <div class="delete-warning mt-3">
                <strong>Внимание:</strong> С изтриването на този град ще бъдат изтрити и всички турнири в него.
                Сигурен ли си, че искаш да продължиш?
            </div>
        </div>

        <form asp-action="DeleteConfirmed" method="post" class="mt-4">
            <input type="hidden" asp-for="Id" />

            <div class="d-grid gap-2 d-sm-flex justify-content-sm-center">
                <button type="submit" class="btn btn-danger btn-lg tg-action-btn px-4">
                    <i class="bi bi-trash3-fill me-2"></i>Confirm Delete
                </button>

                <a asp-action="All" class="btn btn-outline-secondary btn-lg px-4">
                    <i class="bi bi-x-lg me-2"></i>Cancel
                </a>
            </div>
        </form>

    </div>

</section>
